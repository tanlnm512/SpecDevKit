# Context baseline (surveyor-maintained)

The surveyor writes durable codebase docs here on the repo's first spec;
later specs delta-survey instead of re-surveying from zero. This example
repo is two functions — nothing durable beyond survey.md itself yet.
